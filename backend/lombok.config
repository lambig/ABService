# Lombok 生成コードに @lombok.Generated を付与する（1.18.34+ の既定を明示）。
# これにより NullAway が Lombok 生成メソッド（wither/getter/コンストラクタ等）を
# 検査対象外として扱い、生成コード起因の誤検知を避ける。
lombok.addLombokGeneratedAnnotation = true
config.stopBubbling = true
