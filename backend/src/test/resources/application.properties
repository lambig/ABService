# Test Configuration
# 接続先・認証情報は main 側の定義（ホスト・ポート・DB名の単一の出所）をそのまま使う。
# ここで一部だけを上書きすると、Flyway が clean するデータベースとアプリがクエリするデータベースが
# 食い違い得る。

# Flyway: テスト時にデータベースをクリーンにする
%test.quarkus.flyway.clean-at-start=true
