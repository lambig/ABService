/**
 * アセット（画像）アップロードの REST リソース。署名付きURLの発行と確定を受け付ける。JSpecify {@code @NullMarked}:
 * 既定で非 null、null 許容箇所のみ {@code @Nullable} を明示（NullAway で強制）。
 */
@NullMarked
package com.abservice.presentation.rest.asset;

import org.jspecify.annotations.NullMarked;
