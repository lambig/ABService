/**
 * 生成される API 定義（OpenAPI）への反映。エンドポイントごとの注釈を置かず、実装が持つ契約を定義側へ写す。 JSpecify
 * {@code @NullMarked}: 既定で非 null、 null 許容箇所のみ {@code @Nullable} を明示（NullAway
 * で強制）。
 */
@NullMarked
package com.abservice.presentation.rest.openapi;

import org.jspecify.annotations.NullMarked;
