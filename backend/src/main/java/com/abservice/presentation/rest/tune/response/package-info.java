/**
 * チューン集約の REST レスポンス DTO。JSpecify {@code @NullMarked}: 既定で非 null、null 許容箇所のみ
 * {@code @Nullable} を明示（NullAway で強制）。
 */
@NullMarked
package com.abservice.presentation.rest.tune.response;

import org.jspecify.annotations.NullMarked;
