/**
 * 永続化共通。JSpecify {@code @NullMarked}: 既定で非 null、null 許容箇所のみ {@code @Nullable}
 * を明示（NullAway で強制）。
 */
@NullMarked
package com.abservice.infrastructure.persistence;

import org.jspecify.annotations.NullMarked;
