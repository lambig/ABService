/**
 * Album 集約。JSpecify {@code @NullMarked}: 本パッケージの型は既定で非 null、 null 許容箇所のみ
 * {@code @Nullable} を明示する（NullAway で強制）。
 */
@NullMarked
package com.abservice.domain.model.aggregate.album;

import org.jspecify.annotations.NullMarked;
