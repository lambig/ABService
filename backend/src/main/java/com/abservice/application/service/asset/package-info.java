/**
 * アセット（画像）アップロードのユースケース。署名付きURLの発行と、アップロード後の実体検査による確定を担う。 JSpecify
 * {@code @NullMarked}: 既定で非 null、null 許容箇所のみ {@code @Nullable} を明示（NullAway
 * で強制）。
 */
@NullMarked
package com.abservice.application.service.asset;

import org.jspecify.annotations.NullMarked;
