<?xml version="1.0"?>
<ruleset name="ABService PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>PMD rules for ABService project</description>

    <!-- Exclude generated files -->
    <exclude-pattern>.*/target/.*</exclude-pattern>
    <exclude-pattern>.*/build/.*</exclude-pattern>
    <exclude-pattern>.*/generated/.*</exclude-pattern>

    <!-- Java rules -->
    <rule ref="category/java/bestpractices.xml">
        <!-- Exclude some rules that are too strict for Quarkus -->
        <exclude name="JUnitTestContainsTooManyAsserts"/>
        <exclude name="JUnitAssertionsShouldIncludeMessage"/>
    </rule>

    <rule ref="category/java/codestyle.xml">
        <!-- Exclude some rules that conflict with Quarkus conventions -->
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
    </rule>

    <rule ref="category/java/design.xml">
        <!-- Exclude some rules that are too strict -->
        <exclude name="LawOfDemeter"/>
        <exclude name="DataClass"/>
    </rule>

    <rule ref="category/java/errorprone.xml">
        <!-- Keep all error-prone rules -->
    </rule>

    <rule ref="category/java/performance.xml">
        <!-- Keep performance rules -->
    </rule>

    <rule ref="category/java/security.xml">
        <!-- Keep security rules -->
    </rule>

    <!-- Custom rules for Quarkus -->
    <rule ref="category/java/bestpractices.xml/UnusedPrivateField">
        <properties>
            <property name="ignoredAnnotations" value="javax.inject.Inject,io.quarkus.arc.config.ConfigProperty"/>
        </properties>
    </rule>

    <!-- Kotlin specific rules (if using Kotlin) -->
    <rule ref="category/java/codestyle.xml/ShortVariable">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>

</ruleset>
